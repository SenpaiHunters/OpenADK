//

public class SearchManager {
    public init() {}

    // Takes in a search queary and returns a array of results
    public func quearyResults() {}

    // Takes a search query and it returns a url
    // Also handles cases when the queary is a URL and will automaticly direct you
    // rather than using a search engine
    public func search() {}
}
